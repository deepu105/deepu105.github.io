apply plugin: "war"

bootWar {
    mainClassName = "com.jhipster.demo.product.ProductApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
